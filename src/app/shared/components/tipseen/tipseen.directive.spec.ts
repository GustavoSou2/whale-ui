import { TipseenDirective } from '../tipseen.directive';

describe('TipseenDirective', () => {
  it('should create an instance', () => {
    const directive = new TipseenDirective();
    expect(directive).toBeTruthy();
  });
});
