import { CnpjCpfDirective } from '../cnpj-cpf.directive';

describe('CnpjCpfDirective', () => {
  it('should create an instance', () => {
    const directive = new CnpjCpfDirective();
    expect(directive).toBeTruthy();
  });
});
